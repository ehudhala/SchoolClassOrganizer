from setuptools import setup, find_packages

setup(
    name='class_organizer',
    description='Organizes a list of children into classes, where every class has equal qualities of children.',
    packages=find_packages(),
    install_requires=[]
)